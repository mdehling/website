= wrap_layout 'common' do
  .container-lg
    .row.justify-content-center
      %main.col-12.col-xl-10.col-xxl-9.py-3

        - if current_page.data.title
          %h1= current_page.data.title

        = yield
