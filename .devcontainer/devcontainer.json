{
	"name": "dev",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"postCreateCommand": "bundle config path 'vendor/bundle' && bundle install",
	"customizations": {
		"vscode": {
			"settings": {
				"editor.rulers": [80]
			},
			"extensions": [
				"shopify.ruby-lsp"
			]
		}
	}
}
